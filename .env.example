# Default: use the locally installed, authenticated Codex CLI.
AI_PROVIDER=codex
# CODEX_BIN=codex
# Leave unset to use the CLI's default model.
# CODEX_MODEL=

# Alternative: any OpenAI-compatible Chat Completions provider supporting
# strict JSON Schema structured outputs. Keys stay on the server.
# AI_PROVIDER=openai
# AI_API_KEY=
# AI_MODEL=
# AI_BASE_URL=https://api.openai.com/v1

HOST=127.0.0.1
PORT=5173
# DATA_DIR=.data/sequences
# Required when binding beyond loopback; add authentication before public deployment.
# APP_ORIGIN=https://your-computer.example
